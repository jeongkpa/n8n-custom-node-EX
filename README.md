# n8n Custom Node Example

This repository shows a minimal "Hello World" custom node for [n8n](https://n8n.io).

The **HelloWorld** node outputs a greeting using the `Name` parameter.

## Build

```
npm run build
```

This compiles the TypeScript sources to the `dist` folder.
